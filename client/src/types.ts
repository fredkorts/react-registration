export type ResponseObject = {
    email: string,
    first_name: string,
    id: number,
    last_name: string,
    password: string
}

export type UserData = {
    first_name: string,
    last_name: string,
    email: string
}